node_modules
*.log
.DS_Store

# dist/ is committed on purpose: this package is consumed as a git dependency
# (GitHub Packages is blocked by the org billing cap), so the build output must
# be present in the repo for installs that run with --ignore-scripts.
